name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo "Hello World!"
      - name: Run date command 
        run: |
            date
      - name: Run list file command 
        run: |
            ls -l
